test:
machine:
    python:
        version: 3.6.1
dependencies:
    override:
        - pip install -r requirements.txt
        - pip install coveralls
    override:
        - nose2 --with-coverage
    post:
        - coveralls
deployment:
  staging:
    branch: develop
    commands:
      - git fetch origin --unshallow
